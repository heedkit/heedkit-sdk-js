# CDN example

Static HTML file that mounts the widget via a `<script>` tag — no build step,
no npm install. Open `index.html` in a browser.

Edit the `projectKey` in `index.html` to point at your own project.
