---
"@heedkit/sdk-js": patch
---

The Suggest form now inherits the kind the user was browsing — filtering to e.g. Appreciation and hitting "Suggest one →" (or the Suggest tab) preselects Appreciation instead of silently defaulting back to Features.
