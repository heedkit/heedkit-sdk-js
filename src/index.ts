export { FeedbackHubClient } from "./client";
export type {
  Comment,
  EndUser,
  Feature,
  FeedbackHubConfig,
  InitResult,
  Theme,
} from "./client";

export { mount, type MountOptions, type Widget } from "./widget";
